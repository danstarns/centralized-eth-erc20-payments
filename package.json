{
  "scripts": {
    "docker-dev": "docker-compose -f ./docker-compose.yaml -f docker-compose-neo4j.yaml -f docker-compose-redis.yaml -f docker-compose-ganache.yaml --env-file ./.env.ganache.test up --build",
    "docker-test": "docker-compose -f ./docker-compose-test.yaml -f docker-compose-neo4j.yaml -f docker-compose-redis.yaml -f docker-compose-ganache.yaml --env-file ./.env.ganache.test up --build --abort-on-container-exit"
  }
}
